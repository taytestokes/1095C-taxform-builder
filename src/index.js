import React from "react";
import ReactDOM from "react-dom";

// Widgets
import MasterWidget from "./Widgets/Master";

// DOM Render
ReactDOM.render(<MasterWidget />, document.getElementById("root"));
